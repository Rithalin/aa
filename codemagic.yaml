
workflows:
  build:
    name: Build and Test
    max_build_duration: 60
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
